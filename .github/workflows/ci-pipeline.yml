name: CI Pipeline - Python App

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout code
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Set up Python
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      # 3️⃣ Install dependencies
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      # 4️⃣ Linting (Static Code Analysis)
      - name: Lint code with flake8
        run: |
          pip install flake8
          flake8 .

      # 5️⃣ Security Scan (Bandit)
      - name: Bandit Security Scan
        run: |
          pip install bandit
          bandit -r . --severity-level high

      # 6️⃣ Trivy File System Scan
      - name: Trivy FS Scan
        uses: aquasecurity/trivy-action@0.20.0
        with:
          scan-type: fs
          ignore-unfixed: true
          severity: HIGH
          format: table
          output: trivy-fs-scan.txt

      # 7️⃣ Unit tests + coverage
      - name: Run Unit Tests with Coverage
        run: |
          pip install pytest pytest-cov
          # Add current directory to Python path
          export PYTHONPATH=$PYTHONPATH:$(pwd)
          pytest --cov=my_app --cov-report=term-missing --cov-fail-under=80

      # 8️⃣ Build Docker image
      - name: Build Docker image
        run: |
          docker build -t app:latest .

      # 9️⃣ Docker Image Scan
      - name: Scan Docker Image
        run: |
          trivy image --exit-code 1 --severity HIGH,CRITICAL app:latest || true

      # 🔟 Log in to Docker Hub
      - name: Log in to Docker Hub
        run: |
          docker login -u venu95 -p Venu@5198
          docker tag app:latest venu95/git/app:latest
          docker push venu95/git/app:latest      